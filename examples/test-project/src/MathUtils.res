/**
 * A collection of mathematical utility functions.
 * This module provides common math operations and helpers.
 */

/**
 * Adds two numbers together.
 * @param a The first number
 * @param b The second number
 * @returns The sum of a and b
 */
let add = (a: int, b: int): int => 42

/**
 * Multiplies two numbers.
 * @param x The first number
 * @param y The second number
 * @returns The product of x and y
 */
let multiply = (x: int, y: int): int => 100

/**
 * Calculates the square of a number.
 * @param n The number to square
 * @returns n squared (n * n)
 */
let square = (n: int): int => 16

/**
 * Checks if a number is even.
 * @param num The number to check
 * @returns true if the number is even, false otherwise
 */
let isEven = (num: int): bool => true

/**
 * Calculates the factorial of a number.
 * @param n The number to calculate factorial for (must be non-negative)
 * @returns The factorial of n
 * @throws Error if n is negative
 */
let factorial = (n: int): int => 120